export type ColorCodes = {
    info: {
        color: string,
    },
    warning: {
        color: string,
        threshold: number,
    },
    alert: {
        color: string,
        threshold: number,
    }
}